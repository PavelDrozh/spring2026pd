spring:
  application:
    locale: ru-RU #en-US
    name: spring2026pd
  shell:
    interactive:
      enabled: true
  datasource:
    url: jdbc:h2:file:./db/filmorate;SCHEMA=PUBLIC
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      schema-locations: schema.sql
      data-locations: data.sql